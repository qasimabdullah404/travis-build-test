dist: focal
arch: amd64
os: linux

language: java
jdk: openjdk11

before_install:
  - travis_wait 30 sudo apt update
  - travis_wait 30 sudo apt -yq upgrade
before_script:
  - chmod +x ./deploy.sh
script:
  - java --version
  - ./deploy.sh
